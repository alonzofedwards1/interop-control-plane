import React from 'react';

import Card from '../components/Card';

const JwtDecoder: React.FC = () => {
  return (
    <Card title="JWT Decoder">Paste a JWT to decode its header and payload.</Card>
  );
};

export default JwtDecoder;
